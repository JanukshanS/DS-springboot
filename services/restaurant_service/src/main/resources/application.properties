spring.application.name=restaurant_service

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://db.npkdihumxaljttrlpbkh.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=c8T//ZddH+kSfy+
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Port
server.port=8082

# Security Configuration
jwt.secret=foodDeliveryPlatformSecretKeyForRestaurantService
jwt.expiration=86400000

# API Gateway Authentication (disabled by default, enable in production)
auth.gateway.enabled=true
auth.gateway.header=X-User-ID
auth.gateway.role-header=X-User-Roles
