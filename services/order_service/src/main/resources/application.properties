spring.application.name=order_service

# Database Configuration
spring.datasource.url=jdbc:postgresql://db.czczqgoagsqzsgmueyib.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=xD3jdvfF7t1CIHYk
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=8083

# JWT Configuration
jwt.secret=foodDeliveryPlatformSecretKey2025XYZ123456789ABCDEF
jwt.expiration=86400000

# Authentication Gateway Configuration
auth.gateway.enabled=false
auth.gateway.header=X-User-ID
auth.gateway.role-header=X-User-Roles

# Logging Configuration for Security
logging.level.com.order.order_service.security=DEBUG
logging.level.org.springframework.security=INFO
